spring:
  datasource:
    url: jdbc:h2:mem:purchases-test;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop

treasury:
  base-url: http://treasury.test
  connect-timeout-ms: 1000
  read-timeout-ms: 1000

logging:
  level:
    com.wex.purchases: DEBUG
    org.springframework.web: INFO
